function numRegions(graph) {
  const count = 0;
  const visited = new Set();
  let keys = Object.keys(graph);
  while(keys.length !== visited.length){

  }


}

let graph1 = {
  'a': ['b'],
  'b': ['a'],
  'c': ['d'],
  'd': ['e', 'c'],
  'e': ['d'],
};



module.exports = {
    numRegions
};
